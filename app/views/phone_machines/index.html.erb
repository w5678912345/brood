<h1>Listing phone_machines</h1>

<table class="table">
  <tr>
    <th>名字</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @phone_machines.each do |phone_machine| %>
  <tr>
    <td><%= phone_machine.name %></td>
    <td><%= link_to 'Show', phone_machine %></td>
    <td><%= link_to 'Edit', edit_phone_machine_path(phone_machine) %></td>
    <td><%= link_to 'Destroy', phone_machine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Phone machine', new_phone_machine_path %>
