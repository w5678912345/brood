<%= semantic_form_for(@phone_machine) do |f| %>
  <% if @phone_machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_machine.errors.count, "error") %> prohibited this phone_machine from being saved:</h2>

      <ul>
      <% @phone_machine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%=f.input :name,:label => "名字："%>
  <div class="actions">
    <%= f.actions do %>
      <%= f.action :submit %>
    <% end %>
  </div>
<% end %>
