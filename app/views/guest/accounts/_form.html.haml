= form_for @guest_account do |f|
  - if @guest_account.errors.any?
    #error_explanation
      %h2= "#{pluralize(@guest_account.errors.count, "error")} prohibited this guest_account from being saved:"
      %ul
        - @guest_account.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
