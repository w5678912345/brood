<% content_for :title do%>工单列表<%end%>
<%= render "search_form" %>
<div class="alert">
<strong>提示信息:</strong>
<button type="button" class="close" data-dismiss="alert">&times;</button>
<span>查询到<%= @orders.total_entries%>个工单. <%= flash[:msg] if flash[:msg]%></span> 
</div>
<%= render :partial => "table",:locals=>{:orders=>@orders} %>

<%= will_paginate @orders %>