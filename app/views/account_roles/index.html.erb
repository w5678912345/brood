<% content_for :title do%>账号列表<%end%>

<%= render "search_form" %>

<% if params[:tag] == "role"%>
  <%= render :partial => "roles/grid",:locals=>{:roles=>@records} %>
<%else%>
  <%= render :partial => "accounts/form_grid",:locals=>{:accounts=>@records} %>
<%end%>
