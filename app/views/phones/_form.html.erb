<%= semantic_form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%=f.input :id,:label => "号码："%>
  <%=f.input :enabled,:label => "激活"%>
  <%=f.input :phone_machine,:label => "机器："%>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
