@suxu

1. init app
2. design database
3. base functions
4. 


08-15

1. dabi.co 域名绑定(tianyi.dabi.co  test_tianyi.dabi.co  www.dabi.co) => ok
2. 统计 bs ublock fail 的角色 => ok
3. 统计金币 背包余额,转帐记录 => ok
4. 维护computer的指令 => ...


08-16
 1. 与客户端通信的 方案. TCP ,WebSocket, Http ,redis (执行指令) 
 2. 导出上海1区 的账户150个到 test_tianyi
 3. 导出浙江1区 close 24小时的号，到txt
 4. payments 按server 统计
 5. nginx max_collections
 6. 


09-03
.同步api 可以同步角色名
.为角色添加一个ip段,并每周清除 角色的IP段
.控制ip段(前3位)的当前在线数量


09-09
.金币转出统计
.服务器加入 nginx
.整理金币相关菜单
